apply plugin: "war"

bootWar {
    mainClassName = "com.amitkurud.iam.IamApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
