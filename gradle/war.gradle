apply plugin: "war"

bootWar {
    mainClassName = "com.amitkurud.coreconfig.CoreconfigmsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
